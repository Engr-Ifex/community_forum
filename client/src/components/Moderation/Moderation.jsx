import PagePlaceholder from "../common/PagePlaceholder";

const Moderation = () => {
  return (
    <PagePlaceholder
      title="Moderation Page"
      description="Moderation tools will be implemented in a later phase."
    />
  );
};

export default Moderation;
