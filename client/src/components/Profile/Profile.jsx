import PagePlaceholder from "../common/PagePlaceholder";

const Profile = () => {
  return (
    <PagePlaceholder
      title="Profile Page"
      description="User profiles will be implemented in a later phase."
    />
  );
};

export default Profile;
