import PagePlaceholder from "../common/PagePlaceholder";

const Login = () => {
  return (
    <PagePlaceholder
      title="Login Page"
      description="Signing in will be implemented in the authentication phase."
    />
  );
};

export default Login;
